@model List<IdentityChat.Models.SendBoxViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/UILayout/Index.cshtml";
    int sirano = 0;
}


<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Gönderilen Mesajlar</h3>
                <br />
                <br />
                <div class="card-tools">
                    <div class="input-group input-group-sm" style="width: 150px;">
                        <input type="text" name="table_search" class="form-control float-right" placeholder="Search">

                        <div class="input-group-append">
                            <button type="submit" class="btn btn-default">
                                <i class="fas fa-search"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /.card-header -->
            <div class="card-body table-responsive p-0">
                <table class="table table-hover text-nowrap">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Kime</th>
                            <th>Konu</th>
                            <th>Tarih</th>
                            <th>Sil</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var x in Model)
                        {
                            <tr>
                                <td>
                                    @{
                                        sirano++;
                                    } @sirano
                                </td>
                                <td>@x.Name @x.Surname</td>
                                <td><a href="/Inbox/MessageDetail/@x.Id">@x.Subject</a></td>
                                <td>@x.Date.ToShortDateString()</td>
                                <td>
                                    <a class="btn btn-outline-danger btn-sm" href="/SendBox/DeleteSendBox/@x.Id">
                                        <i class="fas fa-trash">
                                        </i>
                                        Sil
                                    </a>
                                </td>
                              

                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>




